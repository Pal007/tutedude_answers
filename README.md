# tutedude_answers
task 1:
i took two number outputs from user and then converted them to float type instead of string type (can also convert to integer type if i type int instead of float).
then coded for addition, subtraction, multiplication and division by writing print function and using +,-,* and / signs.


task 2:
i took two inputs from user for first and last name then used sep = "" to eliminate the space generated between our print function. Then inputed the answers given by the user.
